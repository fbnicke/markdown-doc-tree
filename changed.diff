diff --git a/changed.diff b/changed.diff
new file mode 100644
index 0000000..e69de29
diff --git a/example/docs/1.1_Installation.md b/example/docs/1.1_Installation.md
index 78ebc3c..29b1734 100644
--- a/example/docs/1.1_Installation.md
+++ b/example/docs/1.1_Installation.md
@@ -1,13 +1,6 @@
 # Installation
 
-Install the package:
+Install Markdown Doc Tree:
 
 ```bash
-npm install markdown-doc-tree
-```
-
-Or use CLI directly:
-
-```bash
-npx markdown-doc-tree validate ./docs
-```
\ No newline at end of file
+npm install --save-dev markdown-doc-tree
\ No newline at end of file
diff --git a/package.json b/package.json
index 64f5c1e..bae9cb6 100644
--- a/package.json
+++ b/package.json
@@ -33,9 +33,9 @@
     "dev": "tsx src/index.ts",
     "build": "tsc -p tsconfig.build.json",
     "viewer:dev": "npm run example:manifest && vite",
-    "viewer:build": "npm run example:manifest && vite build",
+    "viewer:build": "npm run example:manifest && vite build && tsx src/clean-viewer-staging.ts",
     "example:validate": "tsx src/cli.ts validate ./example/docs",
-    "example:manifest": "tsx src/cli.ts manifest ./example/docs --output ./output/docs-manifest.json",
+    "example:manifest": "tsx src/cli.ts manifest ./example/docs --output ./output/public/docs-manifest.json",
     "example:pdf": "tsx src/cli.ts pdf ./example/docs --output ./output/manual/documentation.pdf --title \"Markdown Doc Tree Example\""
   },
   "keywords": [
diff --git a/src/clean-viewer-staging.ts b/src/clean-viewer-staging.ts
new file mode 100644
index 0000000..c1d4871
--- /dev/null
+++ b/src/clean-viewer-staging.ts
@@ -0,0 +1,6 @@
+import { rm } from "node:fs/promises";
+
+await rm("./output/public", {
+  recursive: true,
+  force: true,
+});
\ No newline at end of file
diff --git a/src/generate-documentation-manual.ts b/src/generate-documentation-manual.ts
index 2573a36..cca07d1 100644
--- a/src/generate-documentation-manual.ts
+++ b/src/generate-documentation-manual.ts
@@ -55,8 +55,11 @@ export async function generateDocumentationManual(
     rootDirectory,
   );
 
+  const tableOfContents = renderTableOfContents(validation.tree);
+
   const html = createManualHtml({
     title: options.title ?? "Documentation Manual",
+    tableOfContents,
     body,
   });
 
@@ -193,6 +196,7 @@ function assertAssetInsideRoot(
 
 type ManualHtmlOptions = {
   title: string;
+  tableOfContents: string;
   body: string;
 };
 
@@ -330,6 +334,42 @@ function createManualHtml(
       a {
         color: inherit;
       }
+
+      .table-of-contents {
+        break-after: page;
+      }
+
+      .table-of-contents h1 {
+        margin-bottom: 10mm;
+      }
+
+      .table-of-contents-list,
+      .table-of-contents-list ol {
+        margin: 0;
+        padding-left: 0;
+        list-style: none;
+      }
+
+      .table-of-contents-list ol {
+        padding-left: 8mm;
+      }
+
+      .table-of-contents-list li {
+        margin: 2.5mm 0;
+      }
+
+      .table-of-contents-list a {
+        display: flex;
+        gap: 3mm;
+        color: inherit;
+        text-decoration: none;
+      }
+
+      .toc-document-id {
+        min-width: 15mm;
+        color: #64748b;
+        font-variant-numeric: tabular-nums;
+      }
     </style>
   </head>
 
@@ -339,6 +379,11 @@ function createManualHtml(
       <p>Generated with Markdown Doc Tree</p>
     </section>
 
+    <section class="table-of-contents">
+      <h1>Table of Contents</h1>
+      ${options.tableOfContents}
+    </section>
+
     <main>
       ${options.body}
     </main>
@@ -392,3 +437,44 @@ async function renderPdf(
     await browser.close();
   }
 }
+
+function renderTableOfContents(
+  nodes: DocumentNode[],
+): string {
+  return `
+    <ol class="table-of-contents-list">
+      ${nodes
+        .map(renderTableOfContentsNode)
+        .join("\n")}
+    </ol>
+  `;
+}
+
+function renderTableOfContentsNode(
+  node: DocumentNode,
+): string {
+  const children =
+    node.children.length > 0
+      ? `
+        <ol>
+          ${node.children
+            .map(renderTableOfContentsNode)
+            .join("\n")}
+        </ol>
+      `
+      : "";
+
+  return `
+    <li>
+      <a href="#document-${escapeAttribute(node.id)}">
+        <span class="toc-document-id">
+          ${escapeHtml(node.id)}
+        </span>
+
+        <span>${escapeHtml(node.title)}</span>
+      </a>
+
+      ${children}
+    </li>
+  `;
+}
diff --git a/vite.config.ts b/vite.config.ts
index a085bbc..de017ce 100644
--- a/vite.config.ts
+++ b/vite.config.ts
@@ -2,9 +2,9 @@ import { defineConfig } from "vite";
 
 export default defineConfig({
   root: "viewer",
-  publicDir: "../output",
+  publicDir: "../output/public",
   build: {
     outDir: "../output/viewer",
-    emptyOutDir: false,
+    emptyOutDir: true,
   },
 });
