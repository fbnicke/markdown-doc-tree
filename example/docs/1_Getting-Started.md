# Getting Started

Markdown Doc Tree turns a directory of numbered Markdown files into:

- an interactive browser documentation tree
- an embeddable Web Component
- a printable A4 PDF manual

The numeric filename prefix defines both the document order and hierarchy.

**Note:** The viewer and PDF generator use the same Markdown source directory.
