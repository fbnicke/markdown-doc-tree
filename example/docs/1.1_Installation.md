# Installation

Install Markdown Doc Tree as a development dependency:

```bash
npm install --save-dev markdown-doc-tree
```

PDF generation uses Playwright Chromium. Install the browser once:

```bash
npx playwright install chromium
```

You can then invoke the CLI through `npx`:

```bash
npx markdown-doc-tree serve ./docs
```
